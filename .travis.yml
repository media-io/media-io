language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

addons:
  apt:
    sources: ['boost-latest']
    packages:
    - scons

install:
  - ./tools/travis/install_dependencies.sh

script:
  - scons --install-sandbox=`pwd`/dist
